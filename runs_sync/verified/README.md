# Verified runs

This directory contains verified analysis outputs used for Paper 2.

The downstream statistical outputs were regenerated in an isolated verification run from frozen input tables. External catalog metadata were frozen before downstream analysis. Checksums are recorded in each run directory.

These artefacts support reproducibility of the tabulated analyses. They are not, by themselves, evidence for new physics.
